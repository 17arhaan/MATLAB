function result = function1(a, b, c)
    % ADDTHREEVALUES Takes three inputs and returns their sum.
    %    result = ADDTHREEVALUES(a, b, c)
    %
    % Example usage:
    %    total = addThreeValues(1, 5, 10);
    %    disp(total) % 16

    result = a + b + c;
end
